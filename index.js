const processParameters = (argsByTerminal) => {
  const args = {}
  // Procesar argumentos y devolverlos en un objeto, cada parametro debe ser una propiedad del mismo
  return args
}

const actionsDelegated = (action) => {
  // Esta funcion muestra en terminal el retorno de las funciones de books.js
  console.log(action)
}

const main = () => {
  // Recibe los parámetros ingresados por el usuario
  // Llama a processParameters para procesar los parametros
  // Llama a actionsDelegated para hacer las consultas a books.js según los parametros
  // Imprime el resultado de la consuta en la consola
}

main ()